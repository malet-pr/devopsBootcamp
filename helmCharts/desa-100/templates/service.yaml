apiVersion: v1
kind: Service
metadata:
  name: {{ include "desa-100.fullname" . }}
  labels:
    {{- include "desa-100.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "desa-100.selectorLabels" . | nindent 4 }}
